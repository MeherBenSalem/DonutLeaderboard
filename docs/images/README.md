# GUI screenshots

Real in-game PNGs belong here (`category-menu.png`, `leaderboard-populated.png`, `leaderboard-weekly.png`, `leaderboard-page2.png`).

Automated capture on the Cloud Agent VM was attempted (`scripts/capture-gui-screenshots.sh`, log: `build/screenshot-attempt.log`) but did not produce client-rendered images. Capture locally with a Minecraft client against a Paper server with the plugin installed.
